import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  sectionTitle: {
    fontSize: 22,
    fontWeight: "bold",
    marginTop: 18,
    marginBottom: 10,
  },
  genreTitle: {
    fontSize: 20,
    fontWeight: "bold",
    marginTop: 24,
    marginBottom: 8,
  },
  cardWrapper: {
    marginRight: 30,
  },
  flatListContent: {
    paddingHorizontal: 0,
  },
});
